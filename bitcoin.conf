# KEEP these
txindex=1
dbcache=2000

# REMOVE or COMMENT OUT this one (it causes the quit)
# stopatheight=278500

# ADD these to stay offline
connect=0
listen=0

# Enable the "server" so terminal can talk to the app
server=1

# Set a simple username and password for your scripts
rpcuser=ace
rpcpassword=browser

# Ensure it stays on the standard port
rpcport=8332